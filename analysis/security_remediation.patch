# Security remediation patch for RustFileEncryptor
# Apply from project root: git apply analysis/security_remediation.patch
# No credential hunks in this project — security_remediation.local.patch is empty.
#
# Addresses: SEC-001 (CWE-190), SEC-002 (CWE-400), SEC-003 (CWE-323)
--- a/src/main.rs
+++ b/src/main.rs
@@ -92,6 +92,17 @@ fn encrypt_file(input_path: &str, output_path: &str, force: bool) -> std::io::R
     let file_size = std::fs::metadata(input_path)?.len();
     let chunk_count: u64 = if file_size == 0 {
         0
     } else {
         (file_size + CHUNK_SIZE as u64 - 1) / CHUNK_SIZE as u64
     };
+
+    // SEC-001/SEC-003: hard cap at u32::MAX chunks to guarantee nonce uniqueness.
+    // The nonce is [8-byte prefix || 4-byte chunk index], so the counter saturates
+    // at 2^32 - 1 chunks (≈ 256 TiB with 64 KiB chunks).
+    if chunk_count > u32::MAX as u64 {
+        return Err(std::io::Error::new(
+            std::io::ErrorKind::InvalidInput,
+            format!(
+                "File too large: {} chunks exceeds the 2^32 chunk limit (~256 TiB). \
+                 Encrypting beyond this limit would reuse AES-GCM nonces.",
+                chunk_count
+            ),
+        ));
+    }

     let mut input = BufReader::new(File::open(input_path)?);
     let mut output = BufWriter::new(File::create(output_path)?);
@@ -107,7 +118,10 @@ fn encrypt_file(input_path: &str, output_path: &str, force: bool) -> std::io::R
     let mut buf = vec![0u8; CHUNK_SIZE];
     for i in 0..chunk_count {
-        let nonce_bytes = chunk_nonce(&nonce_prefix, i as u32);
+        // SEC-001: chunk_count <= u32::MAX is enforced above; map the error explicitly
+        // rather than panicking so a future refactor that removes the cap surfaces cleanly.
+        let idx = u32::try_from(i).map_err(|_| {
+            std::io::Error::new(std::io::ErrorKind::InvalidInput, "chunk index overflow")
+        })?;
+        let nonce_bytes = chunk_nonce(&nonce_prefix, idx);
         let nonce = Nonce::from_slice(&nonce_bytes);
         let n = read_chunk(&mut input, &mut buf)?;
         let aad = i.to_le_bytes();
@@ -148,6 +162,36 @@ fn decrypt_file(input_path: &str, output_path: &str, force: bool) -> std::io::R
     let mut count_bytes = [0u8; 8];
     input.read_exact(&mut count_bytes)?;
     let chunk_count = u64::from_le_bytes(count_bytes);
+
+    // SEC-002: validate chunk_count against actual file size before entering the
+    // decryption loop. An attacker can set the header field to u64::MAX, which
+    // without this check causes an infinite loop reading past EOF.
+    //
+    // Tight bound: non-last chunks are exactly CHUNK_SIZE+16 bytes. The last chunk
+    // is at least 17 bytes (1 byte plaintext + 16-byte GCM tag). We use CHUNK_SIZE+16
+    // as the divisor, which gives a ceiling that is always >= the true chunk count,
+    // while still rejecting astronomically inflated values.
+    let header_size: u64 = (SALT_LEN + NONCE_PREFIX_LEN + 8) as u64;
+    {
+        let file_size = std::fs::metadata(input_path)?.len();
+        let data_bytes = file_size.saturating_sub(header_size);
+        let max_plausible: u64 = if data_bytes == 0 {
+            0
+        } else {
+            // Minimum encrypted chunk size is CHUNK_SIZE+16; using this as the
+            // divisor gives the tightest safe upper bound on chunk count.
+            data_bytes / (CHUNK_SIZE as u64 + 16) + 1
+        };
+        if chunk_count > max_plausible {
+            return Err(std::io::Error::new(
+                std::io::ErrorKind::InvalidData,
+                format!(
+                    "Corrupt or malicious file: declared {} chunks but file data \
+                     can contain at most {} chunks",
+                    chunk_count, max_plausible
+                ),
+            ));
+        }
+    }
+    // SEC-001 (decrypt path): also enforce the nonce-space cap.
+    if chunk_count > u32::MAX as u64 {
+        return Err(std::io::Error::new(
+            std::io::ErrorKind::InvalidData,
+            "File chunk count exceeds nonce space (must be <= 2^32)".to_string(),
+        ));
+    }

     let password = prompt_password(false);
     let key = derive_key(&password, &salt);
@@ -161,7 +205,10 @@ fn decrypt_file(input_path: &str, output_path: &str, force: bool) -> std::io::R
     let mut buf = vec![0u8; CHUNK_SIZE + 16];

     for i in 0..chunk_count {
-        let nonce_bytes = chunk_nonce(&nonce_prefix, i as u32);
+        // SEC-001: chunk_count <= u32::MAX enforced above; return Err rather than panic.
+        let idx = u32::try_from(i).map_err(|_| {
+            std::io::Error::new(std::io::ErrorKind::InvalidInput, "chunk index overflow")
+        })?;
+        let nonce_bytes = chunk_nonce(&nonce_prefix, idx);
         let nonce = Nonce::from_slice(&nonce_bytes);
         let aad = i.to_le_bytes();
